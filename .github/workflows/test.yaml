name: TEST
on: pull_request

jobs:
  upload-testreport:
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v4
      - run: mv testReport/MiniHILTestReport.html testReport/index.html
      - uses: actions/upload-pages-artifact@v3
        with:
          path: testReport/
  
  deploy:
    permissions:
      pages: write     
      id-token: write 
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: upload-testreport
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
