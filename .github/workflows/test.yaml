name: test 
run-name: ${{ github.actor }} is learning GitHub Actions
on: push

defaults:
  run:
    shell: cmd

jobs:
  print-file:
    runs-on: self-hosted
    steps:
      - run: echo %cd%
        


