name: test 
run-name: ${{ github.actor }} is learning GitHub Actions
on: push

jobs:
  print-file:
    runs-on: self-hosted
    steps:
      - run: cp ../../helloWorld.md .
      - run: cat helloWorld.md

