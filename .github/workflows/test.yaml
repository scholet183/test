name: test 
run-name: ${{ github.actor }} is learning GitHub Actions
on: push

defaults:
  run:
    shell: cmd

jobs:
  #  download-artifact:
  #
  flash:
    runs-on: self-hosted 
    #needs: download-artifact
    steps:
      - name: Build with Gradle
        run: |
          cd C:\miniHIL\TS7x_MotorController_miniHIL\MiniHilPilot
          .\gradlew.bat build
      - name: Flash with Gradle
        run: |
          cd C:\miniHIL\TS7x_MotorController_miniHIL\MiniHilPilot
          .\gradlew.bat flash


